@model IEnumerable<GestionUsarios.Models.TblMateriaHasAlumno>

@{
    ViewData["Title"] = "Index";
    Layout="~/Views/Shared/Template/_Layoutprivate.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FkAlmIdAlumnoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkMtrIdMateriaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FkAlmIdAlumnoNavigation.PkAlmIdAlumno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkMtrIdMateriaNavigation.PkMtrIdMateria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PkAlmHasMtrIdAlumnoHasMateria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PkAlmHasMtrIdAlumnoHasMateria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PkAlmHasMtrIdAlumnoHasMateria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
